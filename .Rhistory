setwd(/Users/fadi_deek/Downloads)
setwd("/Users/fadi_deek/Downloads")
knee <- read.csv("knee.csv")
knee_data <- read.csv("knee.csv")
knee_data %>%group_by(group) %>%summarize(mean_days = mean(days),median_days = median(days),sd_days = sd(days),max = max(days), min = min(days))
library(plyr)
library(tidyverse)
setwd("/Users/fadi_deek/Downloads")
knee_data <- read.csv("knee.csv")
knee_data %>%group_by(group) %>%summarize(mean_days = mean(days),median_days = median(days),sd_days = sd(days),max = max(days), min = min(days))
knee_data <- read.csv("knee.csv")
View(knee_data)
knee_data <- read.csv("knee.csv") %>%gather(key = "group", value = "value",everything())
View(knee_data)
knee_data %>%group_by(group) %>%summarize(mean_days = mean(days),median_days = median(days),sd_days = sd(days),max = max(days), min = min(days))
knee_data <- read.csv("knee.csv") %>%gather(key = "group", value = "days",everything())
knee_data %>%group_by(group) %>%summarize(mean_days = mean(days),median_days = median(days),sd_days = sd(days),max = max(days), min = min(days))
